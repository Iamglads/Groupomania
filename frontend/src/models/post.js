export default class Post {
    constructor(title, content, attachment, userId) {
        this.title = title
        this.content = content
        this.attachment = attachment
        this.userId = userId
    }
}